{{ form_start(myform) }}
<b>{{ form_errors(myform) }}</b>
<table>
    <tr>
        <td>{{ form_row(myform.dateDepart) }}</td>
        <td>{{ form_row(myform.dateArrivee) }}</td>
        <td>{{ form_row(myform.submit) }}</td>
    </tr>
</table>
{{ form_end(myform) }}

<p> recherche par date antérieure </p>

{{ form_start(myform1) }}
{{ form_errors(myform1) }}
<table>
    <tr>
        <td>{{ form_row(myform1.dateAvant) }}</td>
        <td>{{ form_row(myform1.submit) }}</td>
    </tr>
</table>
{{ form_end(myform1) }}