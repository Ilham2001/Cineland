{% extends 'film/layouts/layout.html.twig' %}

{% block title %}Hello FilmController!{% endblock %}

{% block body %}

<div>
    <h4>Liste des films</h4>
    <ul>
        {% for film in films %}
            <a href="{{ path('film_afficher', {'id' : film.id}) }}"><li>Film : {{ film.titre }}</li></a>
        {% endfor %}
    </ul>
</div>
    <div>
        <a href="{{ path('film_ajouter') }}">Ajouter</a>
    </div>
{% endblock %}
